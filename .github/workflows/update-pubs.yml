name: Update publications from CV

on:
  push:
    paths:
      - 'cv.pdf'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-pubs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install pdfplumber pypdf pdf2image Pillow requests

      - name: Install poppler (needed by pdf2image)
        run: sudo apt-get install -y poppler-utils

      - name: Regenerate publications section in index.html
        run: python update_pubs.py --cv cv.pdf --html index.html

      - name: Fetch figures for publications (2016+)
        run: python fetch_figures.py --cv cv.pdf --out figures

      - name: Commit changes
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html figures/
          git diff --cached --quiet || git commit -m "chore: update publications and figures from cv.pdf [skip ci]"
          git push
